version: '2'

services:
  web:
    build:
      context: ..
      dockerfile: Docker/Dockerfile.debug
      args:
      - CONTAINER_PORT=80
      - SERVER_URLS=http://*:80
    ports:
    - "80:80"
    volumes:
    - ..:/app
